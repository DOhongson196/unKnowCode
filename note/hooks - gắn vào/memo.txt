memo() => higher order component (HOC) (bọc bên ngoài component)
useCallback()

memo(component)

memo()  - Dùng để ghi nhớ các props của một component, quyết định xem có render lại component đó hay không 
	để tối ưu về hiệu năng.
	-Ngắn gọn React.memo dùng để xử lý component tránh re-render trong tình huống không cần thiết.
	-nếu có ít nhất một props của component con thay đổi theo component cha => re-render